1. Python version 3.7.3 is used

2. For python lex implementation PLY is used

3. For executing the compiler have to use the follwing command
./exec.sh <filepath> <outputpath>

4. For <filepath> and <outputpath> have to use absolute file paths 